[pytest]
# Limit test collection to the top-level tests/ directory to avoid importing
# packaged test suites (packages/workforce) during local test runs.
# This helps avoid duplicate SQLAlchemy model registration during transitional
# repository layouts where canonical models exist in packages/workforce.
testpaths = tests
