[project]
name = "hospitable-ops"
version = "0.0.1"
description = "Hospitable Ops backend (skeleton)"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
